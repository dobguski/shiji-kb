# 《史记》知识图谱（shiji-kb）数据问题清洗报告

- 版本：`clean/data-cleaning-20260909`
- 日期：2026-09-09
- 数据范围：`wiki/pages.json`、`kg/relations/*.json` 派生的**人物 / 关系数据**（本 fork 数据被下游 `timeline` 站点 enrich 装配后暴露的问题）
- 处置数据落点：下游 `timeline`（私有仓 `dobguski/timeline`）`works.db`；本 fork 未改动任何上游数据文件，仅新增本报告。
- 依据脚本（下游仓）：`data/audit_shiji_dup_20260909.py`、`data/fix_shiji_buwei_20260909.py`、`data/fix_shiji_rel_noise_20260909.py`、`data/merge_shiji_stubs_20260909.py`

---

## 摘要（已核实 · 2026-09-09）

| # | Bug 类别 | 量化 | 状态 |
|---|---|---|---|
| A | 同一人物重复建页：`pages.json` 中某人物的 **aliases 已含别名，别名却又独立成 person 页** | pages 级别名重复对 **484**；装配到下游后不同 id 重复 **346**（对撞精制主档 HIGH 22 / 双空壳 MED 323 / 边界 1） | HIGH 13 条已合并；MED 323 待人工复核 |
| B | `kg/relations` 抽取噪声：**自环**、**年代错位**、**人名碎片当实体** | 自环 **61**（占导入关系 342 的 ~18%）；年代错位（两端断代、无交集）5 | 已删除 |
| C | **误消解**：同一称谓跨朝代/跨人绑定 | 例：「幽王」在《楚世家》指**楚幽王**却消解到西周幽王(≈-771)；幽王系 4 人并存 | 待语境逐条消解 |
| D | `pages.json` aliases **跨人污染** | 例：`舜/帝舜/虞舜` 与 汉·刘舜(常山王) 串在一起 | 判定别名证据需人工复核 |
| E | 下游装配 id 映射缺口（**非本仓库数据缺陷，装配层检出**） | `shiji_event_figures`/`figure_works` 悬挂旧 slug：`yingzheng`/`kongzi`/`li-bai`/`du-fu`… 21 个 | 需映射到库内规范 id |
| F | 下游 json 作者核心独立问题（不在本 fork 修正） | year 倒挂 902；跨代同名（标汉实宋清） | 另立项 |

> 未改动上游任何文件。修正后的下游数据位于私有仓，本报告公开全部方法与证据，供上游及社区复核或回灌。

---

## A. 同一人物重复建页（别名 stub）

**成因**：`pages.json` 对一位人物的 `aliases` 已收录简称/字/谥（如 吕不韦 aliases = `[文信侯, 吕不韦, 不韦]`），却仍为简称另立 `type=person` 空壳页（如 `pages/不韦`：无生卒、quality 23）。

**典型证据**：
- 吕不韦页 aliases 含「不韦」，同时存在 `pages/不韦` 独立 person 页；后者装配成 stub 人物（`?—?·秦`），其唯一事件即 `吕不韦之死`（《秦始皇本纪》 quote：`〖文信侯〗〖不韦〗死`——原文以「不韦」简称本人）。
- 同型高置信例子：`汉高祖`→刘邦、`墨翟`→墨子、`戚姬`→戚夫人、`太史公/子长`→司马迁、`长卿/犬子/司马长卿`→司马相如、`田完`→陈完、`楚王韩信`→韩信、`卫将军`→卫青 等。

**处置**：下游已合并 13 条（含 bu-wei 不韦→吕不韦），全部加 `301 redirect` + 主档 `alt_names` 补别名 + `figure_corrections` 记账；`shiji` 源关系随合并删除（不迁移，防噪声上主档）。

**遗留（待人工，MED 323）**：字/谥号/国名双空壳同人，如 `子路`→`仲由`、`介子推`↔`介之推`、`阖闾`→`公子光`、`周平王`↔`东周平王`、`魏无忌`↔`信陵君`。清单见下游 `data/_audit_buweitype_20260909.csv`。

---

## B. `kg/relations` 抽取噪声

- **自环 61**：`figure_id == related_id`，覆盖全部关系类型（父子/君臣/敌对/祖孙/师徒/兄弟…）。例：`扶苏--父子-->扶苏`、`管仲--推荐-->管仲`、`廉颇--师徒-->廉颇`、`蒙恬--君臣-->蒙恬`。
- **年代错位 5**（两端断代已知、无交集 gap>30 年）：子婴↔李牧、李信↔嬴政(师徒,错型)、齐孝公↔蔡泽(祖孙)、楚怀王↔楚庄王(师徒)、齐襄公↔晋襄公(父子)。
- **人名碎片当实体**：抽取器把 `person1/person2` 填成半句残文（如 `质于赵\n\n[2.1] &秦&$…`），说明原始 NER/关系抽取质量需要整体复审。
- **错型**：`李信--师徒-->嬴政`（实为秦王与将军，非师徒）。

**处置**：下游已删除自环 61（线上 60）+ 年代错位 5（线上 4）。

---

## C. 误消解（称谓跨人/跨朝代绑定）

`kg/relations` 中「幽王」被统一消解为**西周幽王**，但大量语境实指**楚幽王**：

- 《楚世家》：`&秦&相@吕不韦@卒 … @幽王@卒` → 抽取为 `吕不韦--君臣-->幽王`（吕不韦为秦相、幽王为楚王，**无关系**，仅逐年记事并列）。
- 同源噪声还产生了 西周幽王 `自环` 与 `幽王--敌对-->春申君` 等错误边。
- 装配下游后 幽王系 出现 4 个并存人物：`西周幽王`、`周幽王`、`楚幽王`、`郑幽王`。

**处置/遗留**：删除上述已证伪的关系；幽王系其余边界与消解需按文本语境逐条处理，建议做成**实体消解表**回灌。

---

## D. `pages.json` aliases 跨人污染

aliases 列表将通用名/字混入非本人：例 `刘舜`(汉·常山王) 的候选别名串入 神话舜（`舜/帝舜/虞舜` 三个独立 person 页均与 `刘舜` 形成候选重复对）。即 **别名列表不可单独作消歧依据**，需叠加朝代/上下文/关联事件证据。

---

## E. 下游装配 id 映射缺口（本报告附带检出，非本仓库文件缺陷）

下游 enrich 后，若干表悬挂指向 `figures` 不存在的旧 slug id（21 个）：`shiji_event_figures` 含 `yingzheng`(嬴政)、`li-shang-2`；`figure_works` 含 `kongzi/孔子`、`li-bai/李白`、`du-fu/杜甫`、`su-shi/苏轼`、`zhuangzi/庄子` 等；`figure_relations` 含 竹林七贤 向秀/山涛/阮咸。真实人物在库、用的是规范 id，故属**映射缺口**而非脏行（删除会丢数据）。需在装配层补一次「旧 slug → 规范 id」的 re-link。

---

## F. 下游 json 作者核心独立问题（记录，不在本 fork 处理）

与 shiji-kb 数据无关但同批检出：
- **year 倒挂 902 条**（json 源，`birth_year>death_year` 或 `birth>=0 且 death<0`）：如 `宋玉 730..-222`、`李牧 1423..-229`、`李信 578..654`、`张汤 116..-116`——年份字段口径脏，需独立字段审计。
- **跨代同名错配**：`刘旦/刘章/任安/李延年/王信` 等 curated 行 dyn 标「汉」而 bio 为宋/清人（如 燕王旦 stub 对撞的是宋·刘旦而非汉·燕刺王刘旦）。

---

## 处置与复现

下游执行（双端，各 sqlite backup API）：
- 合并 13 条别名 stub → 主档：`data/merge_shiji_stubs_20260909.py`（bu-wei 单独版 `data/fix_shiji_buwei_20260909.py`）
- 删 shiji 关系自环/年代错位：`data/fix_shiji_rel_noise_20260909.py`
- 全库量化：`data/audit_shiji_dup_20260909.py`
- 备份登记：`docs/备份清单-20260820.txt`；变更日志：`docs/CHANGELOG.md` v5.4.66/67

校验：公网 `/figure/<stub>` 均 301 至主档；`吕不韦` 关系清零；`司马迁/卫青/戚夫人/邹衍` 事件按别名归属并入。

## 许可与归属

- 上游 `baojie/shiji-kb` **无 LICENSE** 声明。本 fork 未改动/未重分发上游数据文件。
- 本报告（本文档）由 dobguski 撰写，采用 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)。
- 本报告所述修正的执行数据位于私有仓 `dobguski/timeline`；如需回灌上游或公开修正后数据包，请在上游授权形式明确后另行处理。

## 待核清单索引（下游私有仓）

- MED 双空壳同人 323：`data/_audit_buweitype_20260909.csv`
- 关系噪声（年代错位）：`data/_audit_relartifacts_20260909.csv`
- 合并计划：`data/_shiji_stubmerge_plan_20260909.csv`

---

## 附：`data-cleaning/` 数据包（v0.2-clean-20260909）

除本报告外，本分支新增机器可读数据包（**不改上游数据文件**），目录见 `data-cleaning/README.md`：

| 文件 | 内容 |
|---|---|
| `data-cleaning/merges.csv` | 已执行同人合并 13 条（含证据） |
| `data-cleaning/relations_removed.csv` | 已删关系 70 条（自环 61 + 年代错位 5 + 别名自指/幽王伪关系，行级 rel_id） |
| `data-cleaning/figure_duplicate_candidates.csv` | 重复候选 346（HIGH 22 / MED 323 / LOW 1） |
| `data-cleaning/excluded_high.csv` | HIGH 排除待核 10（一词双解 / canon 跨代同名） |
| `data-cleaning/id_mapping_gaps.csv` | 装配层旧 slug 悬挂 21（yingzheng/kongzi/li-bai…，勿删需 re-link） |
| `data-cleaning/pages_alias_duplicate_pairs.csv` | pages 别名互指对 484（重复建页根因） |
| `data-cleaning/entity_resolution_pending.csv` | 称谓误消解待核（幽王簇） |
| `data-cleaning/kg_field_quality.json` | **person 字段碎片率：family_relations 86.7% / all_relations 21.9%** |
| `data-cleaning/corrected/` | 修正副本：`pages.clean.json`（折叠 13 重复页）、`kg_*_relations.clean.json`（去自环/别名对/年代错位） |
| `data-cleaning/tools/` | 复现脚本 4 个（audit / fix_buwei / fix_relnoise / merge_stubs） |
| `data-cleaning/METHODS.en.md` | 英文方法说明 |

**新增发现（本包量化）**：`family_relations.json` 原始关系行中 **86.7% 的 person 字段为文本碎片**（标记/半句），即原始关系抽取产物整体质量堪忧；本包**如实报告不删行**，留待上游重加工。详见 `data-cleaning/corrected/README.md` 与 `kg_field_quality.json`。
